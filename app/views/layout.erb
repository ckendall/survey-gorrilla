<!DOCTYPE html>
<html lang="en">
<head>
  <!--
    loads bootstrap
  -->
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <!--
    application.css is where you put your styles
  -->
  <link rel="stylesheet" href="/css/application.css">

  <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
  <script src="/js/bootstrap.min.js"></script>
  <script src="/js/application.js"></script>

  <title></title>
</head>
<body>
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <% if session[:user_id] %>
          <a class="login-logo" href='/users/<%=session[:user_id]%>'><img class="login-logo" src="/imgs/survey_gorilla.png" alt="gorilla logo"></a>
        <% else %>
          <div class="login-logo">
            <img class="login-logo" src="/imgs/survey_gorilla.png" alt="gorilla logo">
          </div>
        <% end %>
      </div>
      <% if session[:user_id] %>
        <div class="navbar-left">
          <form class="navbar-form" action='/users/tweets' method='post'>
            <div class="form-group search">
              <input class="form-control" type='text' name='username'>
              <button type="submit" class="btn btn-default">Search</button>
            </div>
          </form>
        </div>
        <div class="navbar-right">
          <form class="navbar-form" action='/' method='post'>
            <!-- <input type="hidden" name="_method" value="delete"> -->
            <button type='submit' class="btn btn-primary nav-button" value='logout'>Logout</button>
          </form>
        </div>
      <% end %>
    </div>
  </nav>
  <%= yield %>
</body>
</html>
